{
  "capability": "list_datasets",
  "product": "tm",
  "env": "preprod",
  "probed_at": "2026-09-21T00:00:00Z",
  "verdict": "PASS",
  "setup": [
    {
      "step": 1,
      "why": "project scope for the required project_id path param",
      "source": "pool",
      "capability": null,
      "produced": {
        "project_id": 379335744
      }
    }
  ],
  "attempts": [],
  "probe": {
    "arguments": {
      "path_params": {
        "project_id": 379335744
      }
    },
    "status": 200,
    "response_keys": ["success", "datasets", "info"],
    "item_shape": {
      "uuid": "string",
      "name": "string",
      "rows_count": "integer",
      "test_cases_count": "integer",
      "tags": "array",
      "metadata": "object",
      "created_by": {
        "id": "integer",
        "full_name": "string",
        "email": "string",
        "onboarded": "integer",
        "role": "string",
        "reports_and_notification_enabled": "boolean"
      },
      "created_at": "string (date-time)",
      "updated_at": "string (date-time)"
    }
  },
  "contract": {
    "required": ["project_id (path)"],
    "json_path": null,
    "declared_missing": ["created_by.browserstack_user_id", "created_by.group_id"],
    "undeclared_returned": ["tags", "metadata", "created_by.role"]
  },
  "notes": "First attempt succeeded, no retries needed. IMPORTANT for sibling probes (get_dataset, get_dataset_variables, list_dataset_linked_test_cases): the pool project (379335744) is NOT empty of data-driven datasets -- it already contains exactly one: uuid 92d4a614-57d3-4201-8ffc-3f3c9faf9747, name '__probe-dataset-1789734836192', rows_count=2, test_cases_count=0, created_at 2026-09-18T12:33:56Z (created 3 days before this probe, by user id 2522 / probe-user@example.invalid -- looks like leftover state from an earlier probe/seed run, not something this run created). Response was NOT empty (count=1), so the declared item shape was genuinely exercised -> PASS, not UNVERIFIED; EMPTY-CASE RULE does not apply. This directly answers the 'does any dataset exist anywhere in this account' question for the sibling capabilities: yes, at least this one does, in this same pool project, so they can use its uuid without needing to create one (list_datasets is read-only; did not attempt any write/create). Contract check (top-level and one level into created_by only, per shallowest-missing-path rule): the declared 200 schema's dataset item properties omit 'tags' and 'metadata' even though the capability's own top-level 'returns' list explicitly names both (\"...tags, metadata, created_by...\") -- so this is a known documented gap between the JSON schema block and the returns/guidance block, not fresh drift, flagged here as undeclared_returned against the schema strictly. Schema's created_by sub-object declares browserstack_user_id and group_id which were not present in the actual response (declared_missing), while the actual response includes a 'role' field on created_by that the schema does not declare (undeclared_returned) -- did not diff further than this first level into created_by. No pagination beyond page 1 was needed (info.next was null, count=1)."
}
